# قائمة الإطلاق الرسمي

آخر تحديث: 2026-08-02

> `[x]` يعني أن البند تحقّق فعلياً. البنود المفتوحة في هذه القائمة هي إجراءات
> بشرية أو اختبارات مدمّرة/مالية لم تُنفّذ عمداً، وليست ادعاءً بنقص في الكود.

## 1. الكود

- [x] الفحوصات المحلية ناجحة: TypeScript وESLint و113/113 اختباراً وNext build،
  إضافة إلى 22/22 اختبار متصفح على سطح المكتب والجوال.
- [ ] GitHub Actions على إصدار 2026-08-02 ببيئة Node.js 22؛ الفحوصات المحلية
  خضراء، ويُغلق هذا البند بعد رفع فرع الإصدار ونجاح CI عليه.
- [x] لا توجد ثغرات معروفة في `pnpm audit --prod --audit-level=high`.
- [x] النشر الإنتاجي مبني من فرع وcommit معروفين ويمكن الرجوع إليه.

## 2. قاعدة البيانات

- [x] طُبقت `db/migrations/20260730_security_hardening.sql` على Supabase الإنتاج
  مرتين بلا خطأ. أعادت `modaafa_security_posture()` قيمة `ok=true`، وبقي جدول
  `pending_oauth_sessions` معزولاً عن أدوار المتصفح بدلاً من إسقاطه بلا نسخة احتياطية.
- [x] طُبقت `db/migrations/20260802_job_capacity.sql` على Supabase الإنتاج مرتين
  بلا خطأ؛ أعمدة مؤشرات الطوابير موجودة ولا يستطيع `anon` أو `authenticated`
  تحديثها مباشرة.
- [x] كل ملفات `db/migrations/` الأقدم مطبقة على Supabase الإنتاج.
- [x] RLS مفعّل، واختبار الكتابة غير المصرح بها على الفوترة مرفوض.
- [x] الجداول الجديدة موجودة: `oauth_states`, `usage_events`, `job_runs`, `processed_webhook_events`, `billing_trial_grants`, `rate_limit_windows`.
- [x] وظائف القياس وحدود الطلبات تعمل عبر service role.

## 3. Google

- [x] redirect URI للدخول: `https://ai.modaafa.com/api/auth/google/callback`.
- [x] redirect URI لربط الإعلانات: `https://ai.modaafa.com/api/auth/google-ads/callback`.
- [x] الدومين والصفحة الرئيسية والخصوصية والشروط وحذف البيانات ظاهرة في شاشة الموافقة.
- [x] مراجعة OAuth مكتملة فعلياً، و`GOOGLE_OAUTH_APP_VERIFIED=true` مضبوط في الإنتاج.
- [x] اختبار MCC كبير؛ اكتُشفت الحسابات العميلة وظهرت أسماؤها وإمكانية تبديل الحساب.
- [x] لا تطلب المنصة metrics من MCC نفسه، مع حاجز واختبارات آلية.

## 4. Stripe

- [x] الحساب في live mode ويستقبل المدفوعات وبياناته مكتملة.
- [x] أسعار الخطط الستة فعالة، متكررة، بالريال والفترة الصحيحة وفي live mode.
- [x] webhook الإنتاج مفعل ويشمل كل الأحداث المطلوبة.
- [x] أحداث الاشتراك تُطبّق بترتيب زمني محمي، وتُقرأ الحالة الحالية من Stripe
  قبل الحفظ حتى لا يعيد حدث قديم اشتراكاً ملغى إلى حالة نشطة.
- [ ] تجربة الدفع، الرجوع، webhook، البوابة، الإلغاء وفشل الدفع في **Stripe test mode**.
  لم تُرسل دفعة حقيقية عمداً، ولا يُختبر هذا البند على مفاتيح live.
- [x] منع تكرار التجربة والفاتورة مطبّق بقيود دائمة واختبارات آلية.

## 5. الذكاء والتشغيل

- [x] فحص Anthropic يجد 11 نموذجاً متاحاً ويختار Claude Sonnet 5 للتقارير.
- [x] يظهر للمستخدم تنبيه واضح إذا استخدم الرد الاحتياطي.
- [x] مسارات `sync-google-ads` و`optimize` محمية وتعمل؛ وهي تعالج فقط الأنشطة
  ذات الاشتراك المستحق. توحّدت قاعدة الاستحقاق في 2026-07-30 لتشمل
  `past_due` حتى نهاية الفترة المدفوعة.
- [x] نطاق Resend موثق والبريد التشغيلي مضبوط.
- [x] فحص الصحة المفصل يرجع `launch_ready: true`.

## 6. تجربة العميل

- [x] مستخدم جديد: دخول Google وربط Ads واختيار الحساب.
- [x] مستخدم جديد عبر رابط البريد: إنشاء الحساب، حفظ النشاط، الانتقال للربط،
  دخول لوحة فارغة واضحة، ثم حذف الحساب التجريبي نهائياً وإنهاء جلسته.
- [x] مستخدم عائد يدخل لنفس المساحة ولا يعاد إلى onboarding.
- [x] التبديل بين الحسابات يحفظ الحساب المختار ويحدّث الصفحات المرتبطة به.
- [x] كل نموذج بطيء يعرض حالة انتظار ويمنع النقر المتكرر.
- [x] الجوال وسطح المكتب اجتازا جولة لقطات بلا تجاوز أفقي أو نص متداخل.
- [ ] أول مزامنة وفحص ومحادثة كاملة بحساب يملك اشتراكاً نشطاً.
- [x] حذف حساب بلا اشتراك أو ربط Google حُقّق حياً من الواجهة حتى إزالة الجلسة.
- [ ] حذف حساب تجريبي يملك اشتراك Stripe test وربط Google تجريبي لإثبات مساري
  الإلغاء والإبطال معاً؛ لا يُنفّذ على حساب حقيقي.

## 7. إطلاق محدود

- [ ] ابدأ بعدد صغير من العملاء لمدة 7 أيام.
- [ ] راقب الأخطاء، زمن الاستجابة، تكاليف Anthropic، معدلات مزامنة Google، وwebhooks Stripe يومياً.
- [ ] لا توسع التسجيل العام قبل إغلاق أي فشل في الربط أو الدفع أو تنفيذ التوصيات.
