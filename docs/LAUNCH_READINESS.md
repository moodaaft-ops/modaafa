# تقرير جاهزية الإطلاق — مُضاعِف (Modaafa Ads AI)

- التاريخ: 2026-07-30
- المُعِدّ: جلسات المراجعة والإصلاح الهندسية (Claude + Codex)
- النطاق: `https://ai.modaafa.com`
- الحكم: **جاهز تقنياً للإطلاق العام المتدرّج.** اكتملت مراجعة Google، وفحص
  الصحة الإنتاجي أخضر. البند المالي المتبقي هو جولة Stripe كاملة في وضع
  الاختبار؛ لا تُرسل دفعة live لإغلاقه.
- المرجعان المكمّلان: [قائمة الإطلاق](LAUNCH-CHECKLIST.md) و[دليل التشغيل](OPERATIONS-RUNBOOK.md)

> هذا التقرير يستبدل نسخة 2026-07-22. ما ورد هناك عن «كل الفحوصات خضراء» كان
> صحيحاً على مستوى الأدوات، لكن المراجعة الحالية وجدت ثغرات لا تظهر في
> `tsc` أو `eslint`: أخطرها أن أي مستخدم مسجَّل كان يستطيع منح نفسه خطة
> مدفوعة مجاناً عبر مفتاح Supabase العام.

## تحديث حاسم — 2026-07-30

- **وافقت Google على OAuth** واكتملت بنود Verification Center، وضُبط
  `GOOGLE_OAUTH_APP_VERIFIED=true` في الإنتاج.
- فحص `/api/health` المفصل أعاد `launch_ready: true` بلا blockers أو warnings.
- القراءة الحية من Google Ads نجحت على حساب عميل غير إداري، بما فيها مقاييس
  آخر 7 أيام، من دون استعلام metrics على حساب MCC.
- جُرّبت رحلة مستخدم جديد عبر رابط البريد فعلياً: إنشاء → حفظ نشاط → لوحة
  التحكم → حذف نهائي → منع الوصول للجلسة المحذوفة.
- وحّدت مهام المزامنة والتحسين قاعدة الاستحقاق مع الواجهة: اشتراك
  `past_due` يظل مؤهلاً حتى نهاية الفترة المدفوعة.
- شُدّد استقبال أحداث Stripe: كل حدث يعيد قراءة حالة الاشتراك الحالية من
  Stripe، ويُحفظ بحارس زمني متسامح مع التكرار والتزامن، فلا يستطيع حدث قديم
  إعادة اشتراك ملغى إلى حالة نشطة.
- حُذفت متغيرات تشغيل قديمة وغير مستخدمة من Vercel
  (`MOYASAR_*`, `VERTEX_AI_REGION`, و`STRIPE_PRICE_ID` القديم).

---

## 1) ملخص بنية المشروع

- **Next.js 15.5 (App Router) + React 18 + TypeScript + Tailwind**، عربي أولاً RTL مع وضع فاتح/داكن كامل.
- **Supabase**: مصادقة + Postgres مع RLS مفعّل على كل الجداول (20 جدولاً بعد اليوم). العزل بين المستأجرين يعتمد على سلسلة `auth.uid()` → `businesses` → `google_ads_accounts`.
- **OAuth مزدوج منفصل**: دخول المنصة (`/api/auth/google/*`، نطاق openid/email/profile) وربط Google Ads (`/api/auth/google-ads/*`، نطاق adwords وحيد)، مع حالة CSRF مخزنة server-side أحادية الاستخدام (جدول `oauth_states`).
- **Google Ads** عبر REST wrapper مخصص (gRPC لا يعمل على Vercel): اكتشاف شجرة MCC، ترقيم صفحات pageToken، أسماء عبر `descriptive_name` مع سلسلة fallback، وحاجز قبل أي استعلام metrics يمنع الحسابات الإدارية.
- **المساعد الذكي**: Anthropic API مع اكتشاف نماذج ديناميكي، وتاريخ محادثة محفوظ server-side، وسياق حقيقي (الحساب المختار + كاش الحملات + آخر فحص + التوصيات)، وعزل بيانات Google Ads داخل `<account_data>` كبيانات لا كتعليمات.
- **مركز الموافقات**: التوصيات تمر بحالات pending → approved → executing → applied مع قفل تنفيذ (`execution_key`)، وvalidateOnly قبل التنفيذ، وحواجز أمان للميزانية والمزايدة، وسجل rollback، وعرض صريح لما سيتغيّر فعلياً قبل الاعتماد.
- **Stripe**: 3 خطط × شهري/سنوي، تجربة 14 يوماً بمنحة واحدة لكل مستخدم وسجل دائم لا يُحذف مع الحساب، webhook موقّع مع منع تكرار الأحداث وحارس ترتيب، وCustomer Portal، وفواتير داخل المنصة.
- **التشغيل**: GitHub Actions يستدعي وظائف الإنتاج كل ساعة (المزامنة عند الدقيقة 17 والتحسين عند الدقيقة 47)، بينما يستضيف Vercel المسارات فقط. الاستدعاءات محمية بـ `CRON_SECRET` timing-safe وميزانية زمنية صريحة، مع حدود معدل، حدود استخدام حسب الخطة، بريد تشغيلي عبر Resend، وفحص صحة مفصل `/api/health`.
- **الحذف النهائي**: تأكيد نصي صريح → إلغاء اشتراكات Stripe → **إبطال refresh tokens في Google كخطوة حاجزة** → حذف مستخدم المصادقة (والتتالي يحذف بقية البيانات).

## 2) ما تم إصلاحه في جلسة 2026-07-26

### P0 — ثغرات أمنية قابلة للاستغلال

| # | الإصلاح | الأثر قبل الإصلاح | الملفات |
|---|---|---|---|
| 1 | **`subscriptions` و`invoices` أصبحتا للقراءة فقط من العميل** | سياسة `FOR ALL` في Postgres تُطبَّق على INSERT/UPDATE أيضاً. أي مستخدم مسجَّل يأخذ مفتاح anon العام من الـ bundle ويضيف صفاً `plan:'pro', status:'active'` لنفسه → كل الخصائص المدفوعة مجاناً، ويستطيع تزوير أو حذف الفواتير | `db/migrations/20260726_launch_hardening.sql`, `db/schema.sql` |
| 2 | **الكوكي لم يعد ينقذ `user_mismatch` في ربط Google Ads** | كان الكوكي مربوطاً بالمتصفح لا بالمستخدم. مستخدم A يبدأ الربط، يخرج، يدخل B على نفس المتصفح، A يكمل الموافقة → refresh token وحسابات A تُكتب داخل نشاط B | `app/api/auth/google-ads/callback/route.ts` |
| 3 | **إغلاق open redirect بعد تسجيل الدخول** | `getSafeNextPath` لم يرفض الـ backslash؛ المتصفحات تعامل `\` كـ `/`، فـ `/\evil.com` كان يتحوّل إلى `https://evil.com/` في أكثر لحظة يثق فيها المستخدم | `lib/auth/google-login.ts` |
| 4 | **`Secure` على كوكي جلسة Supabase + HSTS كامل** | مكتبة `@supabase/ssr` لا تضع `secure` افتراضياً؛ وHSTS من Vercel كان بلا `includeSubDomains`/`preload` | `lib/supabase/cookie-options.ts`, `middleware.ts`, `next.config.js` |
| 5 | **تسجيل الخروج يمسح كل كوكيز المنصة** | كان يترك `gads_oauth_state` (ساعة) و`modaafa_selected_customer_id` (90 يوماً) للمستخدم التالي على نفس المتصفح | `lib/auth/session-cookies.ts`, `app/api/auth/signout/route.ts` |
| 6 | **سجلات التنفيذ append-only** | كان بإمكان المستخدم حذف صفوف `ai_actions` لمسح أثر تعديل اعتمده — وهي نفس الصفوف التي يُحسب منها حاجز الميزانية التراكمي 24 ساعة | نفس ملف الهجرة |
| 7 | **حذف الحساب لا يمضي إذا فشل إبطال صلاحية Google** | كان يسجّل تحذيراً ثم يحذف الصف — فتبقى صلاحية Google حيّة لا يستطيع أحد سحبها، بعكس ما تَعِد به صفحة الخصوصية | `app/api/account/delete/route.ts` |

### P0/P1 — وظائف أساسية معطلة

| # | الإصلاح | الأثر قبل الإصلاح | الملفات |
|---|---|---|---|
| 8 | **حاجز قبل أي استعلام metrics على حساب إداري** | `is_manager` كان يُقرأ عند الاكتشاف ولا يُخزَّن أبداً، ولا يوجد أي حارس عند الاستعلام. حساب MCC يتسلل مرة واحدة (خطأ عابر عند قراءة بياناته) فيفشل كل sync إلى الأبد بـ `REQUESTED_METRICS_FOR_MANAGER` بلا مسار تعافٍ | `lib/google-ads/sync.ts`, `lib/accounts/selection.ts`, الهجرة |
| 9 | **إصلاح تعقيد اكتشاف الحسابات** | `customer_client` متعدٍّ (transitive) — استعلام واحد من الجذر يرجع كل الشجرة. الكود كان يعيد الاستعلام لكل حساب إداري فرعي (41 استعلاماً متطابقاً لـ MCC فيه 40 مديراً)، ثم يعيد مسح الشجرة كاملة **لكل حساب** داخل حلقة الـ cron | `lib/google-ads/client.ts`, `app/api/cron/sync-google-ads/route.ts` |
| 10 | **مهلة زمنية وإعادة محاولة على كل نداء Google Ads** | لا مهلة إطلاقاً: أي socket معلّق يستهلك الـ 300 ثانية كاملة. و`JSON.parse` كان يسبق فحص `response.ok`، فردّ HTML بـ 502 يتحول إلى `SyntaxError` لا يستطيع أي مصنّف أخطاء قراءته | `lib/google-ads/client.ts` |
| 11 | **ميزانية زمنية للمهام المجدولة + سقف دفعة** | مهمة التحسين كانت تسحب كل الحسابات بلا حد وتعالجها بالتسلسل داخل 300 ثانية؛ تُقتل في المنتصف، `finishJobRun` لا يُنفَّذ، ونفس الحسابات تُتخطى كل ليلة | `app/api/cron/{optimize,sync-google-ads}/route.ts`, `lib/platform/concurrency.ts` |
| 12 | **نشاط واحد لكل مستخدم (قيد فريد + upsert)** | نقرتان على «التالي» تُنشئان نشاطاً ثانياً؛ وكل القراءات تأخذ الأحدث، فتصبح حسابات المستخدم المربوطة غير مرئية ويُعاد إلى onboarding — وهذا هو السبب الأرجح لشكوى «المستخدم القديم يرجع للإعداد من البداية» | الهجرة, `app/api/onboarding/business/route.ts`, `app/api/auth/google-ads/callback/route.ts` |
| 13 | **إعادة الربط لم تعد تمسح الأسماء المُدخلة يدوياً** | قراءة البيانات القديمة كانت تُرجع Map فارغة عند أي خطأ (وتُقتطع عند 1000 صف)، فيكتب الـ upsert `NULL` فوق كل اسم وعملة ومنطقة زمنية | `app/api/auth/google-ads/callback/route.ts` |
| 14 | **زر «التالي/تنفيذ/فحص…» يُظهر التحميل ويمنع النقر المتكرر** | `useFormStatus` لا يعمل إلا مع Server Actions، وكل النماذج هنا تُرسل إلى رابط نصي — فـ `pending` كان `false` دائماً في ~20 زراً، بينها «تنفيذ» و«التراجع» اللذان يعدّلان Google Ads فعلياً | `lib/ui/pending-submit-button.tsx` |
| 15 | **حاجز `adjust_bid` (±20%)** | لم يكن موجوداً أصلاً رغم أن الـ prompt ورأس الملف يَعِدان به. قيمة `target_cpa_micros: 1` تمرّ من الحواجز ومن `validateOnly` معاً وتُسقط عرض الحساب بالكامل | `lib/ai/optimizer-agent.ts`, `app/api/recommendations/action/route.ts` |
| 16 | **مركز الموافقات يعرض التعديل الفعلي** | كان يعرض جملة عربية كتبها النموذج فقط؛ `action_payload` — الكائن الذي ينفَّذ فعلاً — لا يُقرأ ولا يُعرض | `app/(dashboard)/optimizer/page.tsx` |
| 17 | **عزل نصوص Google Ads داخل `<account_data>`** | مصطلحات البحث يكتبها أي شخص يبحث بعبارة تُشغّل الإعلان؛ كانت تدخل الـ prompt بلا حماية، ومخرجات النموذج تصبح نص الموافقة الذي يقرأه صاحب الحساب | `lib/ai/optimizer-agent.ts`, `app/api/chat/assistant/route.ts` |
| 18 | **مصنّف النية لم يعد ينشئ مسودات وهمية** | كان يطابق كلمة «حملة» وحدها، فسؤال «كيف أداء حملة الرياض؟» يُنشئ صفاً `pending` في مركز الموافقات — خمسة أسئلة تدفن التوصيات الحقيقية | `app/api/chat/assistant/route.ts` |
| 19 | **المساعد لا يعلّق إلى الأبد** | `await fetch` بلا try/catch؛ أي انقطاع شبكة يترك `loading = true` نهائياً ويعطّل زر الإرسال، والحل الوحيد إعادة تحميل الصفحة | `app/(dashboard)/assistant/assistant-client.tsx` |
| 20 | **`updateMask` بصيغة camelCase** | الحمولة تُحوَّل إلى camelCase بينما الـ mask يبقى `amount_micros` — وFieldMask في proto3 JSON لا يقبل الشرطة السفلية، فتعديلات الميزانية والمزايدة معرّضة للرفض | `lib/google-ads/client.ts` |
| 21 | **البحث في مبدّل الحسابات يعمل فعلاً** | `''.includes('')` يساوي true، فأي بحث بلا أرقام كان يُرجع كل الحسابات — البحث كان معطلاً بالكامل للنص العربي | `app/(dashboard)/account-switcher.tsx` |

### P1 — الفوترة

| # | الإصلاح | الأثر قبل الإصلاح | الملفات |
|---|---|---|---|
| 22 | **إنشاء Stripe Customer قبل الـ checkout** | `customer_email` في وضع الاشتراك يجعل Stripe يُنشئ عميلاً جديداً لكل جلسة مكتملة؛ نقرتان متزامنتان = اشتراكان حيّان وخصمان | `lib/billing/stripe.ts`, `app/api/billing/{checkout,start-trial}/route.ts` |
| 23 | **الخطة تُشتق من السعر لا من metadata** | metadata لقطة تُكتب مرة واحدة ولا يحدّثها Stripe؛ من ينزل من pro إلى starter عبر البوابة يبقى بحدود pro وهو يدفع سعر starter | `lib/billing/stripe.ts`, `app/api/webhooks/stripe/route.ts` |
| 24 | **الويبهوك لا يردّ 200 على حدث ما زال قيد التنفيذ** | Stripe يعتبر أي 2xx تسليماً ناجحاً ويتوقف عن إعادة المحاولة. حدث ماتت محاولته الأولى في المنتصف يُفقد نهائياً — و`customer.subscription.deleted` مفقود يعني وصولاً مجانياً بلا نهاية | `app/api/webhooks/stripe/route.ts` |
| 25 | **حارس ترتيب الأحداث (`last_event_at`)** | Stripe لا يضمن الترتيب؛ حدث `updated` قديم يصل بعد `deleted` كان يعيد كتابة `active` فوق اشتراك ملغى | نفس الملف + الهجرة |
| 26 | **سجل تجارب دائم لا يُحذف مع الحساب** | `billing_trial_grants` مرتبط بـ `users.id` ويُحذف بالتتالي؛ حذف الحساب وإعادة التسجيل = تجارب مجانية 14 يوماً بلا حد | `lib/billing/checkout-policy.ts` + الهجرة |
| 27 | **`past_due` يحتفظ بالوصول حتى نهاية الفترة المدفوعة** | أول محاولة خصم فاشلة كانت تقطع المساعد والفحص والمزامنة عن عميل ما زال يدفع وStripe ما زال يحاول | `lib/billing/entitlements.ts` |
| 28 | **الصلاحية تُحسب من الاشتراك الحيّ لا الأحدث** | إلغاء اشتراك مكرر يجعل الصف الملغى هو الأحدث، فيُحرم عميل يدفع من كل شيء | نفس الملف |
| 29 | **فحص وضع Stripe الحيّ وفواصل الفترة والتجربة المدمجة في السعر** | `livemode` كان يُبلَّغ ولا يُفرَض، فنشرة إنتاج بمفاتيح اختبار تقول `launch_ready: true` | `lib/billing/stripe.ts` |
| 30 | **حذف `STRIPE_PRICE_ID` كقيمة احتياطية** | متغيّر واحد ناقص كان يجعل كل الخطط تُحصَّل بنفس السعر، وفحص الصحة يفحص القيمة *بعد* الاحتياطي فلا يبلّغ عن شيء | `lib/billing/stripe.ts`, `.env.example` |
| 31 | **`trial_will_end` + عملة الفاتورة + فهرس فريد للفواتير** | لا إشعار قبل أول خصم؛ ومبالغ بعملات أخرى تُعرض على أنها ريال؛ ومنع التكرار كان read-then-write بلا قيد | `app/api/webhooks/stripe/route.ts`, `lib/notifications/email.ts`, الهجرة |

### P1/P2 — تجربة المستخدم

| # | الإصلاح | الملفات |
|---|---|---|
| 32 | **المستخدم الجديد يمرّ بـ onboarding فعلاً** (لم يكن أي مسار يقود إليه) | `app/(dashboard)/layout.tsx` |
| 33 | **إشارات النجاح تُعرض**: الاشتراك، ربط Google، الاعتماد، التجاهل، المزامنة في `/campaigns` | `app/(dashboard)/{dashboard,campaigns,optimizer}/page.tsx` |
| 34 | **أخطاء حدود المعدل لم تعد صفحة JSON بيضاء** على مسارات يدخلها المستخدم بتصفح كامل | `app/api/auth/google/login`, `.../google-ads/connect`, `.../accounts/sync`, `.../onboarding/business` |
| 35 | **رسائل خطأ دقيقة للمزامنة** (انتهاء الصلاحية، صلاحيات ناقصة، حساب إداري، حساب معطّل) بدل رسالة واحدة عامة | `lib/ui/sync-errors.ts` |
| 36 | **لوحة جاهزية الإطلاق لم تعد أول ما يراه العميل** — صارت خلف `MODAAFA_OPERATOR_EMAILS` | `app/(dashboard)/settings/page.tsx` |
| 37 | **اختيار الحساب في المساعد لم يعد يتسابق**، ومبدّل الحساب معطّل أثناء الطلب | `app/(dashboard)/assistant/assistant-client.tsx` |
| 38 | **إصلاحات RTL منطقية** (17 موضعاً: `ms-/me-/ps-/pe-/start-/end-/text-start`) — أحدها كان مكسوراً فعلياً في العربية (هياكل التحميل)، والبقية كانت ستنكسر فور تفعيل الإنجليزية | 17 ملفاً |
| 39 | **إصلاحات الوضع الداكن**: 5 حالات كُتب فيها `dark:` خارج الحالة (فتظهر دائماً) — أسوأها أن **خيارات «الهدف الأساسي» الأربعة كانت تبدو كلها مختارة**؛ و9 أسطح فاتحة بلا مقابل داكن؛ ونص غير مقروء | `app/onboarding/business/page.tsx` وغيره |
| 40 | **تباين الزر الأساسي** من ~2.6:1 إلى ما يتجاوز 4.5:1 (WCAG AA) | `tailwind.config.ts` |
| 41 | **الجوال**: `100dvh` بدل `100vh`، كسر الأسطر للبريد والمعرفات والروابط الطويلة | 5 ملفات |
| 42 | **العربية فقط داخل الشارات والأزرار** (كانت «مفعلة / Enabled» داخل شارة `whitespace-nowrap`)، والمصطلح الإنجليزي متاح كـ tooltip | `lib/ui/labels.ts` |
| 43 | **صورة مشاركة 1200×630 PNG** (كانت SVG، وهي غير صالحة لأي شبكة اجتماعية) + بطاقة Twitter | `public/og-image.png`, `app/layout.tsx` |
| 44 | **الدرج على الجوال**: `role="dialog"`، إغلاق بـ Escape، انزلاق من الجهة الصحيحة في الاتجاهين | `app/(dashboard)/dashboard-chrome.tsx` |
| 45 | **رسائل Supabase الإنجليزية تُترجم** في نموذج الدخول، وتأكيد حذف الحساب يظهر بعد الحذف | `app/(auth)/login/page.tsx` |

### إعادة بناء الواجهات بالكامل (طلب منفصل، نفس الجلسة)

الاتجاه المتفق عليه: **Linear / Vercel — داكن أولاً، حاد ومكثّف**. النطاق: كل شيء (تعريفية + دخول + onboarding + كل لوحة التحكم).

**نظام التصميم**

| # | ما تغيّر | لماذا |
|---|---|---|
| 46 | **الداكن صار الافتراضي من الخادم** — `<html class="dark">` وسكربت الإقلاع يزيل الصنف فقط لمن اختار الفاتح صراحةً | إضافة `dark` من السكربت كانت تعني إطاراً واحداً فاتحاً في كل تحميل. الأهم: `:root` بقي فاتحاً و`.dark` داكناً حتى تظل مئات أدوات `dark:` القائمة تعمل بدل إعادة كتابتها |
| 47 | **رموز أسطح جديدة**: `--background-elevated` و`--foreground-subtle` و`--border-strong` و`--edge-highlight` | الارتفاع في واجهة شبه سوداء لا يأتي من ظل — الظل غير مرئي هناك — بل من حد شعري + خط إضاءة داخلي بمقدار 1px |
| 48 | **أربعة أسطح مسمّاة** (`surface-card` / `surface-raised` / `surface-sunken` / `surface-interactive`) بدل تكرار `rounded-lg border bg-card` في كل ملف | كانت الأسطح تختلف من صفحة لأخرى بلا سبب |
| 49 | **`lib/ui/field.tsx` جديد**: تعريف واحد للحقول والقوائم والمناطق النصية + مكوّن `Field` | كل نموذج كان يعرّف حقوله بنفسه: الإعدادات `h-10`، onboarding `h-11`، منتقي الفحص شكل ثالث، تأكيد الحذف رابع. وحلقة التركيز صارت ring لأن تغيير لون الحد وحده غير مرئي تقريباً على خلفية داكنة |
| 50 | **الأرقام والتواريخ بأرقام لاتينية** (`ar-SA-u-nu-latn`) والتقويم ميلادي صراحةً (`u-ca-gregory`) | كان التاريخ يظهر هجرياً على أجهزة عربية — رقم لا يطابق ما يراه العميل في Google Ads ولا في فاتورة Stripe |

**إصلاحات فعلية اكتُشفت أثناء التحقق البصري (لقطات، لا قراءة كود)**

| # | العطل | السبب |
|---|---|---|
| 51 | **الشريط الجانبي كان يختفي كلياً على الكمبيوتر** | `rtl:translate-x-full` مع `lg:translate-x-0`: متغيّر `rtl:` يُترجم إلى `[dir=rtl] .rtl\:…` بخصوصية (0,2,0) تتغلب على قاعدة `lg:` التي لا تحمل إلا media query (0,1,0). الحل: `max-lg:rtl:` لحصر التحويل بالشاشات الصغيرة |
| 52 | **درج الجوال كان ظاهراً وهو مُغلق** | `inset-y-0 end-0`: في RTL الحافة `end` هي **اليسار**، فـ `translate-x-full` (+X) كان يدفع الدرج إلى **داخل** الشاشة بدل خارجها. صُحّح إلى `start-0` |
| 53 | **شريط أخضر بعرض الصفحة تحت الترويسة** | زخرفة `absolute inset-x-0` داخل الشريط الجانبي، والشريط `lg:static` — والعنصر الـ static ليس containing block، فهربت الزخرفة وحُسبت مقابل الصفحة كلها. صُحّح إلى `lg:relative` (تخطيط مطابق، لكنه يعيد تثبيت الابن) |
| 54 | **الوضع الفاتح كان مستحيلاً** | `dark` مثبّت في `<html>` بينما السكربت يضيفه أيضاً، فلا شيء يزيله أبداً |

**الشاشات**

| # | ما تغيّر |
|---|---|
| 55 | **الصفحة التعريفية** أُعيدت بالكامل: hero على وهج شعاعي + شبكة، تدفق 5 خطوات، 6 بطاقات قدرات، لوحة ثقة، تسعير بمزايا لكل خطة تقود إلى `/login?next=/billing?plan=…`، 6 أسئلة شائعة، تذييل |
| 56 | **onboarding: 4 خطوات → خطوتان** — «اختيار الحساب» و«أول فحص» لم تكونا خطوتين أصلاً بل وجهتين داخل المنتج، فمن أنهى الإعداد كان يقرأ «الخطوة 2 من 4» ويستنتج أنه في منتصف الطريق |
| 57 | **مخرج من طريق `no_client_accounts` المسدود** — كانت رسالة حمراء واحدة تقول «اربط بريداً يملك حساب عميل» بلا أي إجراء، وإعادة المحاولة بنفس البريد تعطي نفس الخطأ إلى الأبد. صار هناك ثلاثة مسارات فعلية: بريد Google آخر، إنشاء حساب عميل تحت الحساب الإداري (برابط مباشر)، أو طلب دعوة من مالك الحساب |
| 58 | **رابط «لوحة التحكم» في ترويسة onboarding** لم يعد يظهر للمستخدم الجديد — كان يقذفه إلى لوحة فارغة. المخرج الآن داخل شريط التقدّم ولا يظهر إلا لمن يملك حساباً فعلاً |
| 59 | **حالات فارغة قابلة للتنفيذ** في 6 مواضع كانت سطر نص رمادي: مركز الموافقات (×2)، الفحص، الفواتير، الحسابات المربوطة |
| 60 | **اتجاه فقاعات المحادثة كان معكوساً** — رسائل المستخدم كانت تظهر حيث يتوقع القارئ العربي رسائل الطرف الآخر. الوارد الآن عند بداية القراءة (اليمين في RTL) والصادر عند نهايتها |
| 61 | **رد المساعد الاحتياطي يعيد صياغة السؤال أولاً** — كان يفتح بنفس ملخص الحساب مهما كان السؤال، فيبدو أن أحداً لم يقرأ الطلب. الصياغة تُطبَّق على المسار المعروض للمستخدم فقط ولا تدخل `<account_data>` حفاظاً على عزل الـ prompt |
| 62 | **بطاقات الخطط في الفوترة** تُظهر «خطتك الحالية» وتبدّل الزر إلى «إدارة هذه الخطة»، وتاريخ التجديد يظهر بدل سطر فارغ |

### CSP بالـ nonce (بموافقتك — كل الموقع)

| # | ما تغيّر | الملفات |
|---|---|---|
| 63 | **`script-src` صار `'self' 'nonce-…' 'strict-dynamic'` بدل `'unsafe-inline'`** | `lib/security/csp.ts` (جديد), `middleware.ts`, `next.config.js`, `app/layout.tsx` |

`'unsafe-inline'` في `script-src` يجعل بقية السياسة زخرفة: من يستطيع حقن وسم `<script>` يستطيع حقن محتواه أيضاً. الـ nonce يتغيّر كل طلب، فلا يمكن أن يُكتب في `next.config.js` — انتقلت السياسة كاملة إلى `middleware.ts`، وتُركت بقية ترويسات الأمان في مكانها.

`'strict-dynamic'` هو ما يجعل هذا عملياً مع Next: Next يحقن سكربتات bootstrap مضمّنة ثم يحمّل منها ملفات المسار، فيسري وثوق الـ nonce إلى ما تحمّله ولا يحتاج كل chunk إلى nonce خاص. `style-src` أبقيت فيه `'unsafe-inline'` عمداً — Next ومحمّل الخطوط يخرجان كتل `<style>` لا تقبل nonce، والقاعدة المحقونة سلاح أضعف بكثير من السكربت المحقون.

**كلفة القياس الفعلي** (`next start` محلي، 15 طلباً لكل حالة، الوسيط):

| | TTFB وسيط | p90 |
|---|---|---|
| قبل — الصفحة التعريفية ثابتة | **11 ms** | 13 ms |
| بعد — nonce وتصيير ديناميكي | **37 ms** | 50 ms |

الفرق محلياً ‎+26 ms. على Vercel الفرق أكبر: الصفحة التعريفية كانت تُخدَم من الـ CDN عند الحافة، وصارت استدعاء دالة في منطقة واحدة — فأضف زمن الشبكة من السعودية إلى منطقة الدالة، وبداية باردة أحياناً. هذا هو الثمن الذي وافقت عليه مقابل إغلاق `'unsafe-inline'` على كل الموقع.

**إن أردت التراجع لاحقاً لنصف الطريق** (لوحة التحكم بالـ nonce والصفحات العامة ثابتة): أعِد `Content-Security-Policy` الثابتة إلى `next.config.js` للمسارات العامة، واحصر `matcher` في `middleware.ts` على المسارات المحمية. لا شيء آخر يتغيّر.

**تحقق فعلي:** 5 مسارات (`/`, `/login`, `/privacy`, `/terms`, 404) في متصفح حقيقي — كل وسم `<script>` يحمل nonce (43/43 على التعريفية)، الصفحات تُميّه (hydrate)، **صفر انتهاك CSP في الـ console**. و6 اختبارات جديدة تثبت أن `'unsafe-inline'` لا يعود إلى `script-src`، وأن الـ nonce فريد و128 بت ولا يحوي محرفاً يكسر الترويسة.

### تنظيف

- حذف `lib/billing/moyasar.ts` (لا مستورد، لا مسار webhook، ومخطط توقيع HMAC غير مُتحقَّق منه مقابل المزوّد).
- **حذف مسار `select-account` الميت بالكامل**: `app/onboarding/select-account/` و`app/api/auth/google-ads/select-account/`. لا شيء كان يوجّه إليهما بعد الربط التلقائي، لكن المسار كان يظل نقطة نهاية OAuth قابلة للوصول تكتب في `google_ads_accounts` اعتماداً على معرّف جلسة لا يُنشئه أحد. حُذف أيضاً فحصه في `/api/health`؛ **الجدول `pending_oauth_sessions` نفسه لم يُحذف** — إسقاط جدول قرار هجرة غير قابل للتراجع، ويُترك لك.
- **حذف `lib/ai/audit-agent.ts` و`lib/google-ads/audit-queries.ts`**: مسار فحص بالنموذج لم يُستدعَ قط (12 استعلام GAQL عبر gRPC + نداء نموذج بـ 8000 توكن، بلا اختبار ولا حاجز). الفحص العامل هو `runRuleBasedAudit` الحتمي. النوع `AuditResult` — الجزء الوحيد الذي له مستهلك حيّ — نُقل إلى `lib/audit/rule-engine.ts`.
- إضافة `_to_delete/` إلى `.gitignore`.
- **إزالة `.git/index.lock` عالق منذ 2026-07-01** — كان يمنع كل commit بصمت، ولهذا بقي عمل شهر كامل خارج git.

## 3) نتائج الفحوصات (آخر إعادة: 2026-07-30)

| الفحص | النتيجة |
|---|---|
| `tsc --noEmit` | ✅ exit 0 |
| `eslint . --max-warnings=0` | ✅ exit 0 |
| الاختبارات (`tsx --test`) | ✅ **78/78** |
| `next build` (إنتاجي) | ✅ exit 0 — كل المسارات تُبنى |
| `pnpm audit --prod --audit-level=high` | ✅ لا توجد ثغرات معروفة بعد تحديث Next.js وSharp وPostCSS |
| تشغيل إنتاجي محلي + لقطات | ✅ 23 لقطة (فاتح/داكن × 1440px/390px، وسبع نقاط تمرير): **تجاوز أفقي = 0 في كل لقطة**، `dir=rtl` صحيح، لا أخطاء console |
| ترويسات الأمان محلياً | ✅ HSTS كامل + CSP + Permissions-Policy المحدثة |

التحقق البصري لم يكن قراءة كود: شُغّل الخادم الإنتاجي محلياً والتُقطت اللقطات بـ Playwright ثم **قُرئت كصور**. أربعة أعطال (البنود 51–54) لم تكن لتظهر بأي طريقة أخرى — `tsc` و`eslint` و`next build` كانت خضراء بينما الشريط الجانبي غير موجود على الكمبيوتر.

**الاختبارات الجديدة تغطي**: open redirect بالـ backslash، تعدد حالات OAuth للتبويبات المتعددة، صيغة `updateMask`، حدود التزامن والميزانية الزمنية، تنظيف حقن الـ prompt (عربي وإنجليزي)، مفتاح سجل التجارب، اختيار الاشتراك الحيّ (مكرر/`past_due`/منتهٍ)، وحواجز `adjust_bid` الست.

**بيئة الجهاز**: الفحوصات تمر محلياً. يظهر تحذير engine فقط عند استخدام Node
24 لأن المشروع يثبت Node 22 في `package.json` وCI/Vercel؛ التحذير لا يغيّر
نتيجة الفحوصات.

## 4) ما تم اختباره فعلياً — وما لم يُختبر

**اختُبر:**
- بناء إنتاجي كامل + تشغيل الخادم الإنتاجي محلياً.
- الصفحة التعريفية، الدخول، الخصوصية، 404 — على 1440px و375px، في الوضعين، بلقطات.
- `robots.txt`, `sitemap.xml`, `favicon.ico`, `og-image.png`, `manifest.webmanifest` — كلها 200 محلياً.
- ترويسات الأمان في الرد الفعلي.
- رحلة OAuth الحية بحساب `moodaaft@gmail.com`، واكتشاف حسابات MCC والتبديل بينها.
- قراءة حملات ومقاييس حية من حساب عميل غير إداري عبر Google Ads API.
- تسجيل مستخدم جديد عبر رابط البريد، حفظ بيانات النشاط، ثم الحذف النهائي وإنهاء الجلسة.
- الفحص المفصل `launch_ready: true`، وحالة Stripe live والأسعار والويبهوك قراءةً.

**لم يُختبر عمداً:**
- تدفق Stripe المالي الكامل في test mode؛ بيئة Preview الحالية ليست بيئة staging
  كاملة، ولا يجوز اختبار هذا على مفاتيح live أو إرسال دفعة حقيقية.
- حذف حساب تجريبي يجمع اشتراك Stripe test وربط Google test في نفس الوقت.
- تنفيذ توصية على حملة حقيقية؛ يحتاج إذناً محدداً يذكر الحساب والحملة والتعديل.

## 5) الحالة الحالية للإنتاج (تحقق مباشر اليوم)

`ai.modaafa.com` يعمل على Vercel من فرع `main` عند الإصدار `6a50302`.
المسارات العامة والملفات التعريفية ترجع 200،
والنطاق الجذر يعيد توجيه صفحات الخصوصية والشروط والحذف إلى التطبيق. ترويسات
HSTS وCSP بالـ nonce وPermissions-Policy مفعّلة، ولم تسجل Vercel أخطاء أو
تحذيرات إنتاجية خلال آخر سبعة أيام وقت الفحص.

## 6) المشكلات المتبقية (مرتبة)

- ~~**P0 — الهجرة غير مطبقة**~~ — **أُنجزت في 2026-07-27 قبل النشر**: شُغّلت `20260726_launch_hardening.sql` مرتين بنجاح، وأعاد فحص الأنشطة المكررة صفر صفوف، ورُفض `INSERT` إلى `subscriptions` بدور `anon` بخطأ صلاحيات `42501`.
- ~~**P0 — العمل غير مدفوع إلى GitHub**~~ — **أُنجز**: العمل محفوظ على
  `origin/launch-hardening-2026-07-27` ومنشور على Vercel. بقي دمج `main` عملاً تنظيمياً منفصلاً لأن تاريخيهما
  متباعدان بشدة؛ لا يُستخدم `reset --hard` لحله.
- ~~**P1 — CSP يسمح بـ `'unsafe-inline'`**~~ — **أُصلح بموافقتك على كل الموقع.** التفاصيل والقياس في §2 «CSP بالـ nonce».
- ~~**P1 — `modaafa.com/privacy` و`/terms` 404**~~ — **أُنجز التحسين أيضاً**: الروابط المسجّلة عند Google هي
  `ai.modaafa.com/privacy` و`/terms` وكلاهما يعمل، كما أضيفت إعادة توجيه 301 من النطاق الجذر لمسارات
  `/privacy` و`/terms` و`/data-deletion`.
- **P2 — لا يوجد مبدّل لغة AR/EN**: المنتج عربي أولاً بقرار واعٍ. أصلحت 17 موضع RTL فيزيائياً حتى لا تنكسر الواجهة عند التفعيل، لكن استخراج النصوص وتعميم المنسّقات ما زال عملاً قائماً (يومان إلى ثلاثة).
- ~~**P2 — تدوير `ENCRYPTION_KEY` باب ذو اتجاه واحد**~~ — **أُصلح برمجياً**: النصوص الجديدة تحمل بادئة `v1:`، وفك التشفير يقبل الصيغة القديمة ويجرّب `ENCRYPTION_KEY_PREVIOUS` للقراءة فقط. أضيف `pnpm crypto:rotate` لمسح الصفوف وإعادة تشفيرها على دفعات مع نقطة استئناف وتحديث شرطي يمنع الكتابة فوق تغيير متزامن. تنفيذ التدوير الحي نفسه إجراء تشغيلي مستقل بعد ضبط المفتاح السابق.
- ~~**P2 — ترتيب رسائل المحادثة**~~ — **مُصلح**: `loadPersistedChatHistory` ترتّب بـ `seq` لا `created_at`. الرسالتان تُدرجان في عبارة واحدة فتحملان نفس الطابع الزمني بالميلي ثانية، وكان الترتيب بينهما غير محسوم فقد يُعاد عرض الرد قبل سؤاله.
- **P3 — `pending_oauth_sessions`** جدول لا يُكتب إليه أبداً ويحمل عموداً بشكل token. حُذف المسار الذي كان يستخدمه وحُذف فحصه في `/api/health`، لكن **الجدول نفسه باقٍ**: إسقاطه هجرة غير قابلة للتراجع تُركت لك.

> بندان كانا هنا في النسخة السابقة وأُصلحا اليوم: مسار `select-account` الميت (حُذف)، و«4 خطوات» في شريط onboarding (صار خطوتين).

## 7) العوائق الخارجية (ليست أخطاء كود)

1. **مراجعة Google لشاشة موافقة OAuth — مكتملة** على
   `moodaaft@gmail.com`، مشروع `modaafa-prod`. وصل القبول الصريح من Google
   واكتملت بنود Verification Center، ثم ضُبط
   `GOOGLE_OAUTH_APP_VERIFIED=true`.

2. **وضع Stripe live — متحقق قراءة فقط**: الحساب يقبل المدفوعات والتحويلات، والأسعار الستة live بالريال، ولا فترة
   تجربة مدمجة في تعريف السعر، والويبهوك مفعّل ويشمل الأحداث المطلوبة ومنها
   `customer.subscription.trial_will_end`. لم تُرسل دفعة حقيقية.
3. **نطاق Resend** موثق وتنبيهات التشغيل تصل — يظهر في health المفصل.
4. **صلاحية Google Ads Developer Token** لمستوى الوصول المطلوب (Basic/Standard) حسب حجم الحسابات.

## 8) قائمة التحقق قبل الإطلاق (بالترتيب الإلزامي)

1. [x] **طُبقت الهجرة أولاً** على Supabase الإنتاج:
   `db/migrations/20260726_launch_hardening.sql`
   عبر SQL Editor أو `pnpm db:migrate`. الملف forward-only وقابل لإعادة التشغيل. سيدمج أي أنشطة مكررة قبل إضافة القيد الفريد ويطبع `NOTICE` بعددها.
2. [x] **دُفع الكود ونُشر** من فرع `launch-hardening-2026-07-27`:
   - `git pull --rebase origin main && git push` — أو —
   - `git push origin main:launch-hardening-2026-07-26` ثم افتح PR.
   (لم ألمس حالة الفروع المتباعدة كما طلبت.)
3. [x] شُغّل الفحص المفصل بعد النشر:
   `curl -H "Authorization: Bearer $CRON_SECRET" https://ai.modaafa.com/api/health`
   وكانت كل المكونات سليمة و`launch_ready: true`.
4. [x] تحققت مسارات `/robots.txt` و`/sitemap.xml` و`/favicon.ico` و`/og-image.png` وأصبحت 200.
5. [x] ضُبط `MODAAFA_OPERATOR_EMAILS=moodaaft@gmail.com`.
6. [x] اكتملت مراجعة Google وضُبط `GOOGLE_OAUTH_APP_VERIFIED=true`.
7. [x] أضيفت إعادة توجيه 301 من `modaafa.com/privacy` و`/terms` و`/data-deletion`.
8. [x] جولة حية بحساب `moodaaft@gmail.com`: دخول → ربط Google Ads → ظهور حسابات MCC → بحث وتبديل الحساب → قراءة حملات ومقاييس حساب عميل.
9. [ ] جولة Stripe في وضع الاختبار: checkout → رجوع → webhook → ظهور الاشتراك → Portal → إلغاء. تحقق أن إعادة إرسال نفس الحدث لا تُنشئ فاتورة ثانية.
10. [x] جولة مستخدم بريد تجريبي: تسجيل → onboarding → لوحة → حذف نهائي.
11. [ ] ابدأ بإطلاق متدرج 7 أيام حسب [LAUNCH-CHECKLIST](LAUNCH-CHECKLIST.md) §7، وراقب `job_runs` (خصوصاً `skipped_for_time`) و`processed_webhook_events`.

## 9) الحكم

**جاهز تقنياً للإطلاق العام المتدرّج**: الهجرة والحماية وGoogle OAuth
الموثق وGoogle Ads وStripe live والبنية التشغيلية تعمل في الإنتاج، وكل
الفحوصات الآلية خضراء.

يبقى **اختبار مالي واحد غير منفّذ عمداً**: دورة Stripe كاملة في test mode.
لا يُغلق هذا البند بدفعة حقيقية. كما يبقى الإطلاق المتدرج والمراقبة اليومية
إجراء تشغيل مستمر، لا إصلاح كود.
