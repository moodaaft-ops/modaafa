# برومت جاهز لـ Codex — ما لم أستطع إنجازه في جلسة 2026-07-26

> انسخ كل ما بين الخطين وألصقه لـ Codex كما هو. مكتوب ليكون مستقلاً بذاته: لا يفترض أن Codex قرأ أي محادثة سابقة.

---

## أنت الآن المسؤول الهندسي عن مشروع «مُضاعِف / Modaafa Ads AI»

**المستودع:** `/Users/aimanamin/Documents/GitHub/modaafa`
**الإنتاج:** `https://ai.modaafa.com`
**النطاق الجذر:** `https://modaafa.com`
**حساب Google التشغيلي:** `moodaaft@gmail.com`
**حساب MCC:** `756-114-1000`

المنتج: منصة SaaS عربية أولاً (RTL) تربط حسابات Google Ads، تفحصها، وتقترح تحسينات تمر عبر موافقة صريحة قبل التنفيذ.
البنية: Next.js 15.5 App Router + React 18 + TypeScript + Tailwind + Supabase (Auth/Postgres/RLS) + Google Ads REST API v22 + Stripe + Anthropic API، منشور على Vercel.

### قيود إلزامية — لا تتجاوزها

1. **لا تعرض ولا تطبع ولا تنسخ أي مفتاح أو رمز وصول أو محتوى ملف بيئة** في أي مخرَج أو تقرير أو سجل. استخدم القيم من البيئة عند الحاجة فقط.
2. **لا تفتح ولا تعرض محتوى `google-ads.env`** ولا أي ملف أسرار Google Ads. إذا وجدت مفتاحاً مكشوفاً في أي مكان، **سجّل فقط أنه يحتاج تدويراً — بدون عرضه**.
3. **لا تستخدم `git reset --hard`** ولا أي أمر مدمّر. **لا تصلح حالة الفروع المتباعدة** إلا إذا طُلب منك صراحةً.
4. **لا ترسل أي طلب دفع حقيقي.** اختبارات Stripe في وضع الاختبار فقط.
5. **لا تنشر ولا توقف ولا تعدّل أي حملة Google Ads فعلية** بدون موافقة صريحة من المالك. الاعتماد داخل المنصة مسموح؛ التنفيذ على حساب حقيقي ممنوع بدون إذن.
6. **عند استخدام Chrome استخدم حصراً بروفايل «مضاعفة» المرتبط بـ `moodaaft@gmail.com`.** لا تستخدم أي بروفايل آخر. إذا لم تستطع تحديده بيقين، توقّف واسأل.
7. **لا تسحب metrics مباشرة من الحساب الإداري (MCC)** — Google ترفضها بـ `REQUESTED_METRICS_FOR_MANAGER`، والكود فيه حاجز لهذا فلا تلتف حوله.
8. **لا تدّعِ نجاح أي شيء لم تختبره فعلياً.** إذا لم تُشغّله، قل إنك لم تُشغّله.

### السياق: ما أُنجز قبلك

جلسة هندسية سابقة أصلحت 62 بنداً (سبع ثغرات P0 منها أن أي مستخدم مسجَّل كان يستطيع منح نفسه خطة مدفوعة مجاناً عبر مفتاح Supabase العام)، وأعادت بناء كل الواجهات على نظام تصميم داكن أولاً. التفاصيل الكاملة في **`docs/LAUNCH_READINESS.md`** — **اقرأه أولاً قبل أي شيء آخر**، ومعه `docs/LAUNCH-CHECKLIST.md` و`docs/OPERATIONS-RUNBOOK.md`.

حالة الفحوصات عند تسليمك العمل: `tsc` ✅، `eslint --max-warnings=0` ✅، `tsx --test` ✅ 60/60، `next build` ✅.

**اقرأ أيضاً `docs/GOOGLE-VERIFICATION.md`** — يحوي الحالة الفعلية لمراجعة Google (مفحوصة مباشرةً) وسكربت الفيديو المطلوب ومسودة الرد على Trust & Safety.

**تحذير:** قد يفشل `pnpm test` على الجهاز بخطأ
`esbuild: Host version "0.28.1" does not match binary version "0.28.0"`.
هذا عطل في `node_modules` وليس في الكود — شغّل `pnpm install` أولاً.

---

## المهام — بالترتيب الإلزامي

### 0) أزل أقفال git العالقة (أول شيء، وإلا لن يُحفظ أي عمل)

`.git/index.lock` و`.git/HEAD.lock` كانا عالقين ومنعا كل commit منذ 2026-07-01 بصمت. تحقق ونظّف:

```bash
cd /Users/aimanamin/Documents/GitHub/modaafa
ls -la .git/*.lock .git/objects/*.lock 2>/dev/null
# احذف فقط ما هو حجمه 0 وقديم، وبعد التأكد أنه لا توجد عملية git تعمل:
rm -f .git/index.lock .git/HEAD.lock .git/objects/maintenance.lock
git status
```

**معيار القبول:** `git status` يعمل، و`git commit` تجريبي ينجح.

---

### 1) طبّق الهجرة على Supabase الإنتاج — قبل أي نشر

**الملف:** `db/migrations/20260726_launch_hardening.sql` (forward-only وقابل لإعادة التشغيل).

**هذا هو الترتيب الإلزامي: الهجرة أولاً، ثم النشر.** الكود الجديد يقرأ أعمدة `is_manager` و`google_status` و`last_event_at` وجدول `billing_trial_ledger`. **نشر الكود قبل الهجرة سيكسر لوحة التحكم لكل مستخدم.**

الهجرة تفعل — راجعها بنفسك قبل التشغيل:
- تحويل `subscriptions` و`invoices` إلى قراءة فقط من العميل (`REVOKE INSERT, UPDATE, DELETE`) — **هذا هو إغلاق ثغرة الخطة المجانية**.
- جعل `ai_actions` و`recommendations` غير قابلة للحذف، و`audits` و`reports` غير قابلة للتعديل أو الحذف.
- **دمج الأنشطة المكررة** ثم إضافة `businesses_user_id_key UNIQUE (user_id)`. ستطبع `NOTICE` بعدد ما دمجته — **سجّل هذا الرقم في تقريرك**.
- إضافة `is_manager` و`google_status` و`name_repair_attempted_at` و`subscriptions.last_event_at` و`invoices.currency` و`chat_messages.seq`.
- إنشاء `billing_trial_ledger(email_hash PK)` وفهارس ناقصة.
- `SET search_path` على `handle_new_auth_user`، وRLS على `_modaafa_migrations`.

**نفّذها بإحدى الطريقتين:**
```bash
pnpm db:migrate
# أو الصق الملف في Supabase SQL Editor على مشروع الإنتاج
```

**معيار القبول:**
- الهجرة تكتمل بلا خطأ، وإعادة تشغيلها لا تفشل.
- استعلام تحقق: `SELECT COUNT(*) FROM businesses GROUP BY user_id HAVING COUNT(*) > 1;` يرجع صفر صفوف.
- محاولة INSERT في `subscriptions` بمفتاح anon تُرفض.

---

### 2) ادفع الكود وانشره

الفرع المحلي `main` **متباعد** عن `origin/main` (ahead 2, behind 1)، و`origin/main` لا يحتوي إلا على commit البداية وتعديل README. كل العمل في commits محلية.

**لا تحل التباعد بـ `reset --hard`.** اختر أحد المسارين واذكر أيهما اخترت:
```bash
# (أ) دمج نظيف
git pull --rebase origin main && git push
# (ب) فرع منفصل + PR (الأأمن)
git push origin main:launch-hardening-2026-07-26
```

ثم انشر على Vercel وانتظر اكتمال البناء.

**معيار القبول:** البناء ينجح على Vercel، والـ commit ظاهر على GitHub.

---

### 3) تحقق أن النشر وصل فعلاً

الإنتاج اليوم يعمل بكود **أقدم من 2026-07-22** (`x-vercel-cache: HIT`, `age: 451601`). الدليل الحاسم: هذه المسارات كلها 404 على الإنتاج ويجب أن تصبح 200:

```bash
for p in /robots.txt /sitemap.xml /favicon.ico /icon-192.png /og-image.png /manifest.webmanifest; do
  printf "%s -> " "$p"; curl -s -o /dev/null -w "%{http_code}\n" "https://ai.modaafa.com$p"
done
```

ثم الفحص المفصّل:
```bash
curl -H "Authorization: Bearer $CRON_SECRET" https://ai.modaafa.com/api/health
```
يجب أن يعطي `launch_ready: true`. **إن أعطى false، لا تُكمل — أصلح ما يشكو منه أولاً.** لاحظ أنه يفرض الآن `livemode` على Stripe في الإنتاج وسيرفض المفاتيح التجريبية.

---

### 4) مراجعة Google — فُحصت بالفعل، لا تعِد فحصها

الحالة موثّقة بالكامل في **`docs/GOOGLE-VERIFICATION.md`** (فحص مباشر 2026-07-26، مشروع `modaafa-prod`): ٤ من ٥ بنود مكتملة، والعائق الوحيد **App functionality** وسببه الفيديو التوضيحي ورد على إيميل Trust & Safety — لا علاقة له بالكود.

**دورك هنا محدود:**
- بعد النشر، **جرّب بنفسك** خطوات «HOW TO TEST THE OAUTH CONSENT FLOW» في ذلك الملف من نافذة تصفح خفي حتى شاشة الموافقة، وأبلغ إن تعثرت أي خطوة — لأن المراجع سيتعثر في نفس المكان.
- تحقق أن `ai.modaafa.com/privacy` و`/terms` يرجعان 200 بعد النشر (هما الرابطان المسجّلان عند Google).
- **لا تضبط `GOOGLE_OAUTH_APP_VERIFIED=true`** حتى تتحول البنود الخمسة كلها إلى ✅ في Verification Center. الكود يستخدمها ليقرر عرض تحذير «التطبيق قيد الاختبار» للمستخدم.

تسجيل الفيديو وإرسال الرد على إيميل Trust & Safety مهمة إنسان، لا مهمتك.

---

### 5) تحقق من وضع Stripe الحيّ

**لم أستطع التحقق من هذا للسبب نفسه.**

من لوحة Stripe (بروفايل «مضاعفة»)، تحقق:
- الحساب في وضع live ومفعّل للمدفوعات.
- الخطط الثلاث موجودة بأسعار شهرية وسنوية، وأن `STRIPE_PRICE_*` في Vercel تشير إلى أسعار **live** لا test.
- **لا توجد فترة تجربة مدمجة داخل تعريف السعر نفسه** — المنصة تدير التجربة بنفسها عبر `billing_trial_ledger`، وتجربة مزدوجة تعني 28 يوماً مجاناً بدل 14. الكود يفحص هذا ويرفض، فتأكد أنه يمر.
- الويبهوك مضبوط على `https://ai.modaafa.com/api/webhooks/stripe` ويشمل الأحداث المطلوبة في `REQUIRED_WEBHOOK_EVENTS` داخل `lib/billing/stripe.ts` — **بما فيها `customer.subscription.trial_will_end`**.

**لا ترسل أي دفعة حقيقية.** الاختبار في وضع الاختبار فقط: checkout → رجوع → webhook → ظهور الاشتراك → Portal → إلغاء. وتحقق أن **إعادة إرسال نفس الحدث لا تُنشئ فاتورة ثانية**.

---

### 6) (P3) إعادة توجيه النطاق الجذر — تحسين، ليس عائقاً

**تصحيح لافتراض شائع:** الروابط المسجّلة عند Google هي `https://ai.modaafa.com/privacy` و`/terms` وكلاهما يعمل، وبند «Homepage requirements» اجتاز المراجعة فعلاً. النطاق `modaafa.com` مسجَّل كـ **Authorized domain** فقط، وهذا ما تطلبه Google (النطاق الجذر لا الفرعي). فـ 404 على `modaafa.com/privacy` **ليست عائق مراجعة**.

تبقى مفيدة لمن يكتب `modaafa.com` مباشرة. إن نفّذتها: إعادة توجيه 301 من `modaafa.com/privacy` و`/terms` و`/data-deletion` إلى ما يقابلها على `ai.modaafa.com`، على مستوى DNS/Vercel للنطاق الجذر لا داخل تطبيق Next (التطبيق لا يخدم النطاق الجذر أصلاً).

---

### 7) CSP بالـ nonce — نُفِّذ بالفعل، دورك التحقق فقط

`script-src` صار `'self' 'nonce-…' 'strict-dynamic'` بلا `'unsafe-inline'`. السياسة انتقلت من `next.config.js` إلى `middleware.ts` (`lib/security/csp.ts`)، وكل الصفحات صارت ديناميكية — وهي كلفة موافَق عليها من المالك بعد قياس TTFB (‎11ms → 37ms محلياً).

**المطلوب منك بعد النشر فقط:**
```bash
curl -sI https://ai.modaafa.com/ | grep -i content-security-policy   # يجب أن تحوي nonce- وألا تحوي unsafe-inline في script-src
```
ثم افتح `/` و`/login` و`/dashboard` في متصفح وتأكد أن **console خالٍ من انتهاكات CSP** وأن الصفحات تعمل تفاعلياً. **أي انتهاك يعني سكربتاً بلا nonce — أبلغ عنه ولا تُرجع `'unsafe-inline'` كحل سريع.**

---

### 8) رسائل المحادثة — مُصلحة، لا تعمل عليها

`loadPersistedChatHistory` ترتّب بـ `seq` لا `created_at`. لا شيء مطلوب.

---

### 9) إصدار مفتاح للتشفير (`ENCRYPTION_KEY`) — العمل البرمجي الأساسي المتبقي

**الوضع:** لا يوجد إصدار مفتاح داخل النص المشفّر، فتدوير `ENCRYPTION_KEY` باب ذو اتجاه واحد — كل refresh token مخزّن يصبح غير قابل للفك. الحذف النهائي يتوقف بأمان الآن إذا فشل فك التشفير (بدل أن يترك صلاحية Google معلّقة لا يستطيع أحد سحبها)، لكن التدوير نفسه ما زال مستحيلاً عملياً.

**المطلوب:**
- أضف بادئة إصدار للنص المشفّر (مثلاً `v1:` + الحمولة الحالية) في `lib/crypto` أو ما يقابله.
- اجعل فك التشفير يقبل النصوص القديمة بلا بادئة (يعاملها `v1`) — **هذا شرط عدم كسر البيانات القائمة**.
- ادعم مفتاحاً ثانياً `ENCRYPTION_KEY_PREVIOUS` للقراءة فقط أثناء التدوير.
- اكتب سكربت إعادة تشفير في `scripts/` يمر على الصفوف دفعةً دفعةً، **مع إمكانية الاستئناف ودون إتلاف أي صف عند الفشل**.
- اختبارات: فك تشفير نص قديم بلا بادئة، فك تشفير `v1:`، وفشل نظيف عند مفتاح خاطئ.

---

### 10) تدويل AR/EN (اختياري — بعد الإطلاق)

المنتج عربي أولاً بقرار واعٍ. أُصلح 17 موضع RTL فيزيائياً (`ms-/me-/ps-/pe-/start-/end-/text-start`) حتى لا تنكسر الواجهة عند تفعيل الإنجليزية، لكن استخراج النصوص وتعميم المنسّقات عمل قائم (تقديري: يومان إلى ثلاثة).

إن طُلب منك: استخدم `next-intl`، استخرج النصوص العربية إلى ملفات رسائل، وحوّل `formatCurrency`/`formatNumberAr`/`formatDateAr` في `lib/utils.ts` لتأخذ اللغة كوسيط. **لا تُغيّر الافتراضي عن العربية.**

---

### 11) جولة تحقق حية (بعد كل ما سبق)

بحساب `moodaaft@gmail.com` وبروفايل «مضاعفة» فقط:

1. دخول → onboarding → حفظ بيانات النشاط.
2. ربط Google Ads بموافقة واحدة → **تحقق أن حسابات MCC تظهر بأسمائها لا بأرقامها**.
3. ابحث في مبدّل الحسابات **بنص عربي** (كان معطلاً بالكامل وأُصلح).
4. بدّل الحساب → تحقق أن اللوحة والحملات تتبع الحساب المختار.
5. مزامنة يدوية → تحقق من رسالة النجاح ومن `last_synced_at`.
6. شغّل الفحص → توصيات تظهر.
7. اسأل المساعد **ثلاثة أسئلة مختلفة** (أداء، أولويات، مسودة حملة) وتحقق أن الردود تستخدم أرقام الحساب الفعلية.
8. **اعتمد توصية واحدة — ولا تنفّذها على حملة حقيقية بدون إذن صريح من المالك.**
9. تحقق أن بطاقة «التعديل الفعلي عند التنفيذ» تعرض ما في `action_payload` لا وصفاً نصياً فقط.

**لكل خطوة: اذكر ما رأيته فعلاً. إن لم تُنفّذ خطوة، قل ذلك صراحةً.**

---

### 12) اختياري — إسقاط `pending_oauth_sessions`

الجدول لا يُكتب إليه أبداً بعد حذف مسار `select-account`، ويحمل عموداً بشكل token. المسار والفحص الصحّي حُذفا؛ الجدول باقٍ لأن إسقاطه هجرة غير قابلة للتراجع.

إن قرر المالك إسقاطه: هجرة منفصلة بتاريخها الخاص، **بعد** نسخة احتياطية مؤكدة، مع `DROP TABLE IF EXISTS pending_oauth_sessions CASCADE;` وحذف تعريفه وسياساته من `db/schema.sql`.

---

## المخرَج المطلوب منك

بعد الانتهاء، سلّم تقريراً بست فقرات:

1. **ما أُنجز** — بند بند، مع نتيجة كل معيار قبول.
2. **الملفات التي تغيّرت** — مع سبب كل تغيير في سطر واحد.
3. **نتائج الفحوصات** — مخرجات فعلية لـ `tsc --noEmit` و`eslint . --max-warnings=0` و`pnpm test` و`next build`.
4. **ما لم يكتمل ولماذا** — بصراحة، بلا تلميع.
5. **ما يحتاج تدخّلاً بشرياً** — تسجيل دخول، 2FA، قرار مالي، أو إجراء إنتاجي غير قابل للتراجع.
6. **الروابط الحية التي اختبرتها فعلاً** — مع ما رأيته على كل منها.

**قاعدة أخيرة:** اعمل باستقلالية ولا تنتظر عند كل خطوة. توقّف فقط إذا احتجت تسجيل دخول أو 2FA أو قراراً مالياً أو إجراءً إنتاجياً غير قابل للتراجع. **ولا تدّعِ نجاح أي شيء لم تختبره فعلياً.**

---
