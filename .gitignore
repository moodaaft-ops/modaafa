.DS_Store
node_modules/
.next/
out/
dist/
coverage/
playwright-report/
test-results/
tsconfig.tsbuildinfo

.env
.env.*
!.env.example

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.vercel
.modaafa-reencrypt-progress.json

# Quarantine for files removed through the Cowork device bridge, which cannot
# unlink — deletions are staged here as a move and you remove the folder
# yourself. Never commit it.
_to_delete/
_sync/

# Local exports, verification videos, and generated brand bundles. These can
# exceed a gigabyte and must never enter the application repository or a
# Vercel upload.
artifacts/
